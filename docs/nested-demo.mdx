---
name: Nested Demo
route: /nested-demo
---

import {Playground} from 'docz'
import Collapse from '../src/Collapse';

# Nested Demo

<Collapse>
  {({getCollapsibleProps, getTogglerProps, contentRef}) => (
    <React.Fragment>
      <button {...getTogglerProps()}>Open</button>
      <div
        {...getCollapsibleProps({
          style: {
            borderRadius: 3,
            border: '1px solid paleviolet',
            background: 'lightblue'
          }
        })}
      >
        <div style={{minHeight: 200, border: '1px solid blue'}}>
          <Collapse defaultOpen>
            {({getCollapsibleProps, getTogglerProps, contentRef}) => (
              <React.Fragment>
                <button {...getTogglerProps({style: {display: 'block'}})}>Open</button>
                <div
                  {...getCollapsibleProps({
                    style: {
                      borderRadius: 3,
                      border: '1px solid paleviolet',
                      background: 'yellow'
                    }
                  })}
                >
                  <div
                    style={{
                      borderRadius: '50%',
                      background: 'blue',
                      height: '400px',
                      width: '100%',
                    }}
                  />
                </div>
              </React.Fragment>
            )}
          </Collapse>
          </div>
      </div>
    </React.Fragment>
  )}
</Collapse>